Review harshly for unnecessary complexity and brittle generated-code patterns.

Look for single-use abstractions, duplicated logic, overbroad defensive code, incomplete scaffolding, hidden coupling, and tests that mostly mirror implementation details.
Only raise simplification feedback when it prevents a concrete maintenance or behavior risk.
