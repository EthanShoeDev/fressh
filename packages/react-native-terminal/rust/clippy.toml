# Clippy knobs for the workspace. Lint *levels* live in `[workspace.lints]` in
# Cargo.toml; this file is for clippy's configurable thresholds.
#
# Keep in sync with rust-toolchain.toml's channel.
msrv = "1.82.0"
